name = "runmote-website"
compatibility_date = "2024-11-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# keep install routes via Worker fallback if you later add worker.js
# For now this is static-only. To keep curl | bash working, point runmote.dev/install.sh
# to the separate Worker (runmote) or add a Cloudflare Redirect Rule.

[triggers]
crons = []
